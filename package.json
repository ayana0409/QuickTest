{
  "name": "quicktest-root",
  "version": "1.0.0",
  "private": true,
  "description": "QuickTest Monorepo Root",
  "workspaces": [
    "quick-test-frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=quick-test-frontend",
    "build": "npm run build --workspace=quick-test-frontend",
    "start": "npm run start --workspace=quick-test-frontend",
    "lint": "npm run lint --workspace=quick-test-frontend"
  }
}
