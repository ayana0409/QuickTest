meta {
  name: 9.12 Delete Exam - Bad Request When Published
  type: http
  seq: 12
}

delete {
  url: {{baseUrl}}/api/admin/exams/{{adminExamId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

headers {
  Accept: application/json
}

assert {
  res.status: in [200, 400]
}
