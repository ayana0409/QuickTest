meta {
  name: 3.11 Publish Exam - Success
  type: http
  seq: 11
}

patch {
  url: {{baseUrl}}/api/teacher/exams/{{examId}}/publish
  body: none
  auth: bearer
}

auth:bearer {
  token: {{teacherToken}}
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
}
