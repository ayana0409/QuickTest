meta {
  name: 2.26 Publish Exam - Transition from Closed Success
  type: http
  seq: 26
}

patch {
  url: {{baseUrl}}/api/teacher/exams/{{republishExamId}}/publish
  body: none
  auth: bearer
}

auth:bearer {
  token: {{teacherToken}}
}

headers {
  Accept: application/json
}

assert {
  res.status: eq 200
  res.body.data.status: eq PUBLISHED
}
